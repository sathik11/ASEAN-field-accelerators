name: doc_processing

datasets:
  - name: doc_processing_pr
    source: data/data.jsonl
    description: "This dataset is for pr validation only."
    mappings:
      question: "${data.question}"
      test: "${data.test}"
      customer_id: "${data.customer_id}"

evaluators:
