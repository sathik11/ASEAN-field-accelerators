name: web_researcher

datasets:
  - name: web_researcher_pr
    source: data/data.jsonl
    description: "This dataset is for pr validation only."
    mappings:
      url: "${data.url}"

evaluators:
